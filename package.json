{
  "name": "smartneighbor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test",
    "dev": "node server/index.mjs",
    "smoke": "node -e \"fetch('http://127.0.0.1:3000/api/health').then(r=>r.json()).then(console.log)\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
